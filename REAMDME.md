# GearGuard Backend

FastAPI + PostgreSQL backend for Maintenance Management SaaS.

## Run locally

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

---

## ✅ STATUS CHECK

At this point:

- ✅ App boots
- ✅ Database config ready
- ✅ JWT base ready
- ✅ Clean architecture in place
- ✅ Git‑commit safe

You can already do:

```bash
uvicorn app.main:app --reload

```
